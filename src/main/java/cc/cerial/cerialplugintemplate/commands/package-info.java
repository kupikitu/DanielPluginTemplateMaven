/**
 * This package houses all commands for the plugin.<br>
 * If you are registering commands with no subcommands, directly create classes here.<br>
 * If you are making subcommands, create a package inside of this package with the name of the subcommand
 * (for example: if you are making a command /something with subcommands one and two, you should create
 * a "something" package and then OneCommand and TwoCommand classes.)
 */
package cc.danie.danielplugintemplate.commands;
